/// <reference types="cypress" />

// TODO:
describe.skip("decimals", () => {
  before(() => {
    cy.visit("/");
  });

  beforeEach(() => {
    cy.pressButton("reset");
  });

  it("decimals", () => {
    // decimals
  });
});
